import type { Express } from "express";

// Placeholder hook for Swagger/OpenAPI docs. Wire up swagger-ui-express here if/when
// the dependency is added. Kept minimal to match the layered architecture without
// adding extra runtime weight right now.
export function setupSwagger(_app: Express) {
  // Intentionally empty
}
